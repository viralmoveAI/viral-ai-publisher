/**
 * Standardized cookie name.
 * We use 'session' to ensure maximum compatibility across local development
 * and Vercel production hosting, while keeping it secured via httpOnly and secure flags.
 */
export const SESSION_COOKIE_NAME = "session";

